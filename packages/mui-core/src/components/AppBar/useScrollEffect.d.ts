/**
 * Defaults to window object
 * Optionally can pass another node using id without '#'
 * @param param0
 */
export default function useScrollEffect({ scrollTarget }: {
    scrollTarget: any;
}): boolean;
